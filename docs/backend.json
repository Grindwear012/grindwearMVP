{
  "entities": {
    "Customer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Customer",
      "type": "object",
      "description": "Represents a customer of the e-commerce store, holding their personal details, excluding authentication credentials which are managed externally.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Customer entity. This ID can be linked to an external authentication system's user ID."
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the customer."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the customer."
        },
        "email": {
          "type": "string",
          "description": "The email address of the customer.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "The phone number of the customer."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the customer account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the customer account was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "firstName",
        "lastName",
        "email",
        "createdAt"
      ]
    },
    "Address": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Address",
      "type": "object",
      "description": "Represents a physical address associated with a customer, used for shipping and billing.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Address entity."
        },
        "customerId": {
          "type": "string",
          "description": "Reference to the Customer who owns this address. (Relationship: Customer 1:N Address)"
        },
        "street1": {
          "type": "string",
          "description": "The first line of the street address."
        },
        "street2": {
          "type": "string",
          "description": "The second line of the street address, if applicable."
        },
        "city": {
          "type": "string",
          "description": "The city of the address."
        },
        "state": {
          "type": "string",
          "description": "The state or province of the address."
        },
        "postalCode": {
          "type": "string",
          "description": "The postal or ZIP code of the address."
        },
        "country": {
          "type": "string",
          "description": "The country of the address."
        },
        "addressType": {
          "type": "string",
          "description": "The type of address (e.g., 'shipping', 'billing', 'home', 'work')."
        },
        "isDefaultShipping": {
          "type": "boolean",
          "description": "Indicates if this is the customer's default shipping address."
        },
        "isDefaultBilling": {
          "type": "boolean",
          "description": "Indicates if this is the customer's default billing address."
        }
      },
      "required": [
        "id",
        "customerId",
        "street1",
        "city",
        "state",
        "postalCode",
        "country",
        "addressType"
      ]
    },
    "PaymentMethod": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PaymentMethod",
      "type": "object",
      "description": "Represents a customer's stored payment method, typically a tokenized reference to protect sensitive data.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the PaymentMethod entity."
        },
        "customerId": {
          "type": "string",
          "description": "Reference to the Customer who owns this payment method. (Relationship: Customer 1:N PaymentMethod)"
        },
        "type": {
          "type": "string",
          "description": "The type of payment method (e.g., 'credit_card', 'paypal', 'apple_pay')."
        },
        "provider": {
          "type": "string",
          "description": "The payment provider (e.g., 'Visa', 'Mastercard', 'PayPal')."
        },
        "lastFourDigits": {
          "type": "string",
          "description": "The last four digits of the credit card number, for display purposes."
        },
        "expirationMonth": {
          "type": "number",
          "description": "The expiration month of the payment card."
        },
        "expirationYear": {
          "type": "number",
          "description": "The expiration year of the payment card."
        },
        "cardholderName": {
          "type": "string",
          "description": "The name of the cardholder as it appears on the card."
        },
        "billingAddressId": {
          "type": "string",
          "description": "Reference to the billing address associated with this payment method. (Relationship: Address 1:N PaymentMethod)"
        },
        "token": {
          "type": "string",
          "description": "A token representing the payment method, provided by a PCI-compliant payment gateway. Actual sensitive details are not stored here."
        },
        "isDefault": {
          "type": "boolean",
          "description": "Indicates if this is the customer's default payment method."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the payment method was added.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the payment method was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "customerId",
        "type",
        "provider",
        "token",
        "isDefault",
        "createdAt"
      ]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a single thrift clothing item available for sale in the store.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the clothing item."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the clothing item, including its condition and features."
        },
        "price": {
          "type": "number",
          "description": "The selling price of the item."
        },
        "imageUrls": {
          "type": "array",
          "description": "An array of URLs for images of the product.",
          "items": {
            "type": "string"
          }
        },
        "category": {
          "type": "string",
          "description": "The category of the clothing item (e.g., 'T-shirts', 'Jeans', 'Dresses')."
        },
        "size": {
          "type": "string",
          "description": "The size of the clothing item (e.g., 'S', 'M', 'L', 'XL')."
        },
        "condition": {
          "type": "string",
          "description": "The condition of the thrift item (e.g., 'New with tags', 'Excellent used condition', 'Good used condition')."
        },
        "brand": {
          "type": "string",
          "description": "The brand of the clothing item."
        },
        "color": {
          "type": "string",
          "description": "The primary color of the clothing item."
        },
        "material": {
          "type": "string",
          "description": "The material of the clothing item (e.g., 'Cotton', 'Denim', 'Polyester')."
        },
        "stockQuantity": {
          "type": "number",
          "description": "The number of this item currently in stock."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the product was added to the inventory.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the product details were last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "price",
        "imageUrls",
        "category",
        "size",
        "condition",
        "stockQuantity",
        "createdAt"
      ]
    },
    "Cart": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Cart",
      "type": "object",
      "description": "Represents a customer's shopping cart, holding items selected for purchase.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Cart entity."
        },
        "customerId": {
          "type": "string",
          "description": "Reference to the Customer who owns this cart. (Relationship: Customer 1:N Cart)"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the cart was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the cart was last updated (e.g., item added/removed).",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "customerId",
        "createdAt"
      ]
    },
    "CartItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CartItem",
      "type": "object",
      "description": "Represents a single product item within a shopping cart.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CartItem entity."
        },
        "cartId": {
          "type": "string",
          "description": "Reference to the Cart this item belongs to. (Relationship: Cart 1:N CartItem)"
        },
        "productId": {
          "type": "string",
          "description": "Reference to the Product added to the cart. (Relationship: Product 1:N CartItem)"
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the product in the cart."
        },
        "priceAtAddition": {
          "type": "number",
          "description": "The price of the product at the time it was added to the cart, to handle potential price changes."
        },
        "addedAt": {
          "type": "string",
          "description": "Timestamp when the product was added to the cart.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "cartId",
        "productId",
        "quantity",
        "priceAtAddition",
        "addedAt"
      ]
    },
    "Order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Order",
      "type": "object",
      "description": "Represents a customer's confirmed purchase, containing details about the transaction, shipping, and payment.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Order entity."
        },
        "customerId": {
          "type": "string",
          "description": "Reference to the Customer who placed the order. (Relationship: Customer 1:N Order)"
        },
        "orderDate": {
          "type": "string",
          "description": "The date and time the order was placed.",
          "format": "date-time"
        },
        "totalAmount": {
          "type": "number",
          "description": "The total amount paid for the order, including shipping and taxes."
        },
        "status": {
          "type": "string",
          "description": "The current status of the order (e.g., 'pending', 'processing', 'shipped', 'delivered', 'cancelled')."
        },
        "shippingAddressId": {
          "type": "string",
          "description": "Reference to the Address used for shipping this order. (Relationship: Address 1:N Order)"
        },
        "billingAddressId": {
          "type": "string",
          "description": "Reference to the Address used for billing this order. (Relationship: Address 1:N Order)"
        },
        "paymentMethodId": {
          "type": "string",
          "description": "Reference to the PaymentMethod used for this order. (Relationship: PaymentMethod 1:N Order)"
        },
        "trackingNumber": {
          "type": "string",
          "description": "The tracking number for the shipment, if available."
        },
        "shippingCarrier": {
          "type": "string",
          "description": "The shipping carrier used for the delivery (e.g., 'USPS', 'FedEx', 'UPS')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the order record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the order record was last updated (e.g., status change).",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "customerId",
        "orderDate",
        "totalAmount",
        "status",
        "shippingAddressId",
        "billingAddressId",
        "paymentMethodId",
        "createdAt"
      ]
    },
    "OrderItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OrderItem",
      "type": "object",
      "description": "Represents a single product item included in a specific order.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the OrderItem entity."
        },
        "orderId": {
          "type": "string",
          "description": "Reference to the Order this item belongs to. (Relationship: Order 1:N OrderItem)"
        },
        "productId": {
          "type": "string",
          "description": "Reference to the Product purchased. (Relationship: Product 1:N OrderItem)"
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the product purchased in this order item."
        },
        "priceAtPurchase": {
          "type": "number",
          "description": "The price of the product at the time of purchase, crucial for historical records."
        }
      },
      "required": [
        "id",
        "orderId",
        "productId",
        "quantity",
        "priceAtPurchase"
      ]
    },
    "ProductInteraction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ProductInteraction",
      "type": "object",
      "description": "Records user interactions with products, used for recommendation engine and analytics.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ProductInteraction entity."
        },
        "customerId": {
          "type": "string",
          "description": "Reference to the Customer who performed the interaction. (Relationship: Customer 1:N ProductInteraction)"
        },
        "productId": {
          "type": "string",
          "description": "Reference to the Product with which the customer interacted. (Relationship: Product 1:N ProductInteraction)"
        },
        "interactionType": {
          "type": "string",
          "description": "The type of interaction (e.g., 'view', 'add_to_cart', 'purchase', 'favorite')."
        },
        "timestamp": {
          "type": "string",
          "description": "The timestamp of the interaction.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "customerId",
        "productId",
        "interactionType",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/customers/{customerId}",
        "definition": {
          "entityName": "Customer",
          "schema": {
            "$ref": "#/backend/entities/Customer"
          },
          "description": "Stores customer profile data. Each document is owned by a single authenticated user, identified by `customerId` matching `request.auth.uid`.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique ID of the customer, matching their Firebase Authentication UID."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}/addresses/{addressId}",
        "definition": {
          "entityName": "Address",
          "schema": {
            "$ref": "#/backend/entities/Address"
          },
          "description": "Stores shipping and billing addresses for a specific customer. Each address document's `customerId` is implicitly defined by the path parameter, ensuring direct ownership by `request.auth.uid`.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique ID of the customer who owns this address."
            },
            {
              "name": "addressId",
              "description": "The unique ID for the address."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}/paymentMethods/{paymentMethodId}",
        "definition": {
          "entityName": "PaymentMethod",
          "schema": {
            "$ref": "#/backend/entities/PaymentMethod"
          },
          "description": "Stores tokenized payment methods for a specific customer. Each payment method document's `customerId` is implicitly defined by the path parameter, ensuring direct ownership by `request.auth.uid`.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique ID of the customer who owns this payment method."
            },
            {
              "name": "paymentMethodId",
              "description": "The unique ID for the payment method."
            }
          ]
        }
      },
      {
        "path": "/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Contains public details of all thrift clothing products. This collection is readable by all users and writable only by administrators.",
          "params": [
            {
              "name": "productId",
              "description": "The unique ID of the product."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}/carts/{cartId}",
        "definition": {
          "entityName": "Cart",
          "schema": {
            "$ref": "#/backend/entities/Cart"
          },
          "description": "Stores a customer's shopping cart. Each cart document's `customerId` is implicitly defined by the path parameter, ensuring direct ownership by `request.auth.uid`.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique ID of the customer who owns this cart."
            },
            {
              "name": "cartId",
              "description": "The unique ID for the shopping cart."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}/carts/{cartId}/items/{cartItemId}",
        "definition": {
          "entityName": "CartItem",
          "schema": {
            "$ref": "#/backend/entities/CartItem"
          },
          "description": "Stores individual items within a customer's shopping cart. Each cart item implicitly inherits `customerId` from its parent cart, ensuring direct ownership by `request.auth.uid`.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique ID of the customer who owns the parent cart."
            },
            {
              "name": "cartId",
              "description": "The unique ID of the parent cart."
            },
            {
              "name": "cartItemId",
              "description": "The unique ID for the item within the cart."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}/orders/{orderId}",
        "definition": {
          "entityName": "Order",
          "schema": {
            "$ref": "#/backend/entities/Order"
          },
          "description": "Stores details of a customer's placed orders. Each order document's `customerId` is implicitly defined by the path parameter, ensuring direct ownership by `request.auth.uid` and allowing order management by admins (who can also read this path).",
          "params": [
            {
              "name": "customerId",
              "description": "The unique ID of the customer who placed this order."
            },
            {
              "name": "orderId",
              "description": "The unique ID for the order."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}/orders/{orderId}/items/{orderItemId}",
        "definition": {
          "entityName": "OrderItem",
          "schema": {
            "$ref": "#/backend/entities/OrderItem"
          },
          "description": "Stores individual items within a specific customer order. Each order item implicitly inherits `customerId` from its parent order, ensuring direct ownership by `request.auth.uid`.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique ID of the customer who placed the parent order."
            },
            {
              "name": "orderId",
              "description": "The unique ID of the parent order."
            },
            {
              "name": "orderItemId",
              "description": "The unique ID for the item within the order."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}/productInteractions/{interactionId}",
        "definition": {
          "entityName": "ProductInteraction",
          "schema": {
            "$ref": "#/backend/entities/ProductInteraction"
          },
          "description": "Records customer interactions with products (e.g., views, adds-to-cart). Each interaction document's `customerId` is implicitly defined by the path parameter, enabling personalized recommendations and analytics while ensuring data privacy.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique ID of the customer who performed the interaction."
            },
            {
              "name": "interactionId",
              "description": "The unique ID for the product interaction."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{adminId}",
        "definition": {
          "entityName": "RoleAdmin",
          "schema": {
            "$ref": "#/backend/entities/RoleAdmin"
          },
          "description": "Contains documents where the ID is the `uid` of an administrator. The existence of a document grants admin privileges. Used for DBAC (Database Access Control) to manage products and customer orders. Note: 'RoleAdmin' is a conceptual entity for defining roles, which is not explicitly provided in the initial schema but is crucial for robust authentication.",
          "params": [
            {
              "name": "adminId",
              "description": "The unique ID (UID) of an administrator."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure for 'Thrift Plug' is designed to prioritize secure, scalable, and debuggable authorization using the outlined core principles and mandates. Central to this design is the strict adherence to Authorization Independence and the support for Query-able Access Patterns (QAPs).\n\n1.  Authorization Independence:\n    *   **Path-Based Ownership Denormalization:** For all user-specific data (Customer, Address, PaymentMethod, Cart, CartItem, Order, OrderItem, ProductInteraction), ownership is explicitly denormalized into the document path. For instance, customer addresses are stored at `/customers/{customerId}/addresses/{addressId}`. This ensures that any security rule can verify `request.auth.uid == customerId` directly from the path without requiring any `get()` calls to parent documents. This prevents cascading authorization dependencies, making transactions and batch writes atomic and rules significantly simpler and more performant. It also inherently ties data to its owner, simplifying data isolation.\n    *   **Existence-Based Global Roles:** For administrative access (e.g., managing products, viewing all orders), a dedicated top-level collection `/roles_admin/{adminId}` is used. An administrator's `uid` will be the document ID in this collection. Security rules can check for the existence of `get(/databases/$(database)/documents/roles_admin/$(request.auth.uid)).exists()` to grant elevated privileges. This method is `Authorization Independent` as it does not rely on complex data lookups within the same document or `get()` calls to other non-role-related documents.\n\n2.  QAPs (Rules are not Filters):\n    *   **Public Collections:** The `/products` collection is designed for universal readability. Security rules will grant `read` access to all authenticated users (and potentially unauthenticated users if desired), which naturally supports efficient `list` operations without requiring client-side filtering or server-side rule-based filtering (as all data is meant to be visible).\n    *   **User-Scoped Collections:** For all subcollections under `/customers/{customerId}/...`, `list` operations are inherently secure. A user can only 'list' documents within their specific `/customers/$(request.auth.uid)/...` path. The security rules will enforce `allow list: if request.auth.uid == customerId;`. This automatically scopes the `list` operation to the authenticated user's data, eliminating the need for `where` clauses to enforce security and fully supporting QAPs.\n\n3.  Structural Segregation and Consistency:\n    *   **Private vs. Public Data:** Data is segregated into distinct collections based on its security posture. `products` is public, while `customers/{customerId}/...` subcollections are private to the user.\n    *   **Hierarchical Paths:** User-owned data follows a clear hierarchical path (`/customers/{customerId}/addresses/{addressId}`), making the ownership explicit and simplifying rule writing.\n    *   **Explicit State:** The `Order` entity's `status` field (e.g., 'pending', 'shipped') ensures clear state modeling.\n\nThis architecture minimizes rule complexity, prevents security vulnerabilities arising from `get()` dependencies, and ensures that the application can efficiently query and manage data for individual users and administrators."
  }
}